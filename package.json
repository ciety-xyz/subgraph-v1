{
  "name": "omnuum-v1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ omnuum/omnuum-v1",
    "create-local": "graph create --node http://localhost:8020/ omnuum/omnuum-v1",
    "remove-local": "graph remove --node http://localhost:8020/ omnuum/omnuum-v1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 omnuum/omnuum-v1"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
